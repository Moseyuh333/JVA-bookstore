web: java $JAVA_OPTS -jar target/dependency/webapp-runner.jar --port $PORT target/ROOT.warweb: java $JAVA_OPTS -jar target/dependency/webapp-runner.jar --port $PORT target/ROOT.war# Intentionally left blank. This app uses the Heroku Tomcat buildpack,
# which provides its own default web process for WAR deployment.